# Core geospatial
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
fiona>=1.10
pyarrow>=17.0

# Data acquisition
requests>=2.32
tqdm>=4.66
truststore>=0.9     # OS trust store for TLS (INEP/DATASUS ship broken chains)
pysus>=2.4          # CNES DBC downloads, PR-scoped
pyreaddbc>=0.2      # fallback DBC decoder if pysus breaks

# Road network + isochrones
osmnx>=2.0
networkx>=3.3

# Analysis
pandas>=2.2
numpy>=2.0
scipy>=1.13

# Visualization
matplotlib>=3.9
mapclassify>=2.6
folium>=0.17
branca>=0.7
contextily>=1.6

# Notebook + dev
jupyter>=1.0
jupyterlab>=4.2
ipywidgets>=8.1
python-dotenv>=1.0

# Formatting / lint (optional)
black>=24.4
ruff>=0.5
